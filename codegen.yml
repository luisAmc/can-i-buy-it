overwrite: true
schema: schema.graphql
documents: './src/**/*.tsx'
generates:
  src/graphql/__generated__/generated.ts:
    config:
      scalars:
        DateTime: string
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
